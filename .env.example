# Infrastructure only — runtime API keys live in PostgreSQL (see docs/architecture.md).
APP_ENV=development
HTTP_ADDR=:8080
DATABASE_URL=postgres://koro:koro@localhost:5432/koro?sslmode=disable
REDIS_URL=redis://localhost:6379/0
JWT_SECRET=change-me-in-production-use-long-random-string
